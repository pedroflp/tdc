export const collections = {
  USERS: "users",
  MATCHES: "matches",
  QUEUES: "queues",
  VIDEOS: "videos",
}

export enum remoteConfigs {
  QUEUE_COMPOSITION_MAX_GENERATIONS = "QUEUE_COMPOSITION_MAX_GENERATIONS"
}